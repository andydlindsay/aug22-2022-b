# W07D05 - Custom Hooks

### To Do
- [x] Custom Hooks
- [x] `useDocumentTitle`
- [x] `useMousePosition` (tests)
- [x] `useInput` (tests)
- [x] `useLocationData`
- [x] `useRequest`
- [x] `useKeyPress`

### Custom Hook
* a JS function that uses a hook internally
* logic helper functions


['EMPTY', 'CREATE']
useVisualMode
useApplicationData


```JS
const arr = [1,2,3];

const copy = [...arr];
copy.slice()
```










